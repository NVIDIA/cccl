// Copyright (c) 2026, NVIDIA CORPORATION & AFFILIATES. ALL RIGHTS RESERVED.
//
// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
//
// NVRTC capture excerpt: generated identifiers shortened and whitespace formatted.
// The archive contains the original translation unit and the identifier mapping.
// excerpt-begin
#include <cuda/std/cstdint>

#include <cub/block/block_load.cuh>

using block_load_t =
    cub::BlockLoad<::cuda::std::int32_t, 128, 2,
                   ::cub::BLOCK_LOAD_DIRECT, 1, 1>;

extern "C" __device__ void
load_impl(::cuda::std::int32_t *src,
          ::cuda::std::int32_t *dst) {
  block_load_t().Load(
      src,
      *reinterpret_cast<::cuda::std::int32_t (*)[2]>(
          dst));
}

extern "C" __device__ int
load_abi(void *__ret, void *abi_src, void *abi_dst) {
  ::cuda::std::int32_t *abi_cast_src =
      reinterpret_cast<::cuda::std::int32_t *>(
          abi_src);
  ::cuda::std::int32_t *abi_cast_dst =
      reinterpret_cast<::cuda::std::int32_t *>(
          abi_dst);
  load_impl(abi_cast_src, abi_cast_dst);
  return 0;
}
