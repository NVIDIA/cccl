# cuda.compute benchmarks web report template

Will generate a self-contained web report for the cuda.compute benchmarks.

It will output an html file in the `../results` directory so it can be served quickly from the results directory.

```bash
npm run build
```
